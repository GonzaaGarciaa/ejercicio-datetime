import datetime

hora_actual = datetime.datetime.now()
hora_actual = hora_actual.hour


def tiempo_trabajo(hora):
    if hora > 19:
        print("Es hora de ir a casa")
    else:
        print(f"Todavia quedan {19 - hora} hs de trabajo")


tiempo_trabajo(hora_actual)
